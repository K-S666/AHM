% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_plots.R
\name{compute_Rhat_curve}
\alias{compute_Rhat_curve}
\title{Compute Rhat trajectories over chain length}
\usage{
compute_Rhat_curve(
  result,
  parameters = c("s", "g"),
  step = 1000L,
  min_draws = step,
  max_draws = NULL,
  include_burnin = FALSE
)
}
\arguments{
\item{result}{Output from \code{\link{AHMQ}}.}

\item{parameters}{Chain components passed to \code{\link{compute_Rhat}}.
Defaults to \code{c("s", "g")}.}

\item{step}{Prefix spacing in stored draws.}

\item{min_draws}{First prefix length to evaluate.}

\item{max_draws}{Last prefix length; \code{NULL} uses all stored draws.}

\item{include_burnin}{Whether to include burn-in draws when full chains were
saved with \code{keep_burnin = TRUE}. The default \code{FALSE} computes the
Rhat trajectory from post-burn-in draws.}
}
\value{
A list with \code{max}, a data frame of maximum Rhat by prefix, and
  \code{item}, a data frame of scalar Rhat values for each flattened
  component.
}
\description{
Computes Gelman-Rubin Rhat repeatedly on prefixes of MCMC draws. By default
the recorded burn-in is excluded, matching the final Rhat diagnostics used
in summaries. When \code{include_burnin = TRUE} and \code{\link{AHMQ}} was
run with \code{keep_burnin = TRUE}, prefixes start from iteration 1 of the
full chain and the returned object records the burn-in boundary for plotting.
}
