% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_data.R
\name{simulate_ahmq_data}
\alias{simulate_ahmq_data}
\title{Simulate a complete AHMQ/DINA data set}
\usage{
simulate_ahmq_data(
  N,
  J,
  K,
  Q = NULL,
  G = NULL,
  s = NULL,
  g = NULL,
  pi = NULL,
  alpha = NULL,
  alpha_distribution = c("balanced", "unbalanced"),
  sigma = 0.5,
  s_range = c(0.05, 0.25),
  g_range = c(0.05, 0.25),
  seed = NULL
)
}
\arguments{
\item{N}{Number of examinees.}

\item{J}{Number of items.}

\item{K}{Number of attributes.}

\item{Q}{Optional true Q-matrix. If \code{NULL}, \code{random_Q(J, K)} is
used and then restricted by \code{G}.}

\item{G}{Optional true attribute hierarchy adjacency matrix. If \code{NULL},
the no-prerequisite hierarchy is used.}

\item{s}{Optional slipping vector. If scalar, recycled to length \code{J};
if \code{NULL}, sampled uniformly from \code{s_range}.}

\item{g}{Optional guessing vector. If scalar, recycled to length \code{J};
if \code{NULL}, sampled uniformly from \code{g_range}.}

\item{pi}{Optional latent class probabilities over all \eqn{2^K} classes.
When \code{NULL}, admissible profiles receive equal probability and
inadmissible profiles receive zero probability.}

\item{alpha}{Optional true attribute profile matrix. If supplied, \code{pi}
is ignored for profile generation.}

\item{alpha_distribution}{Attribute profile distribution used when
\code{alpha} is not supplied.}

\item{sigma}{Common off-diagonal covariance/correlation for unbalanced
\code{\link{simu_alpha}} generation.}

\item{s_range, g_range}{Length-two numeric ranges used when \code{s} or
\code{g} are generated.}

\item{seed}{Optional random seed.}
}
\value{
A list containing \code{Y}, \code{Q}, \code{G}, \code{R},
  \code{alpha}, \code{pi}, \code{s}, \code{g}, \code{N}, \code{J}, and
  \code{K}. \code{Y} can be passed directly to \code{\link{AHMQ}}.
}
\description{
Creates a self-contained synthetic data set for testing the Bayesian DINA
estimator with an unknown Q-matrix and unknown attribute hierarchy. The
function can either use supplied true parameters or generate reasonable
defaults: a random Q-matrix, a zero hierarchy, uniform class probabilities
over hierarchy-admissible attribute profiles, and moderate slip/guess
parameters.
}
\examples{
set.seed(1)
dat <- simulate_ahmq_data(N = 50, J = 12, K = 3)
dim(dat$Y)
dat$Q
}
