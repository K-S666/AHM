% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AHMQ_main_fun.R
\name{AHM}
\alias{AHM}
\title{Multi-chain DINA estimation with a known Q-matrix}
\usage{
AHM(
  Y,
  Q,
  N1 = NULL,
  chain_length = 20000L,
  burn_in = 10000L,
  keep_burnin = FALSE,
  chain_num = 4L,
  a_s0 = 1,
  a_g0 = 1,
  b_s0 = 1,
  b_g0 = 1,
  p_add = 0.5,
  parallel = TRUE,
  n_cores = min(chain_num, parallel::detectCores(logical = FALSE)),
  progress = TRUE,
  print_every = 1000L,
  progress_callback = NULL
)
}
\arguments{
\item{Y}{An \eqn{N \times J} binary response matrix.}

\item{Q}{Known binary \eqn{J \times K} Q-matrix.}

\item{N1}{Mini-batch sample size for updating item parameters and G. Q is
fixed in this model. If \code{NULL}, the default is \code{floor(N / 2)},
where \code{N} is the number of examinees.}

\item{chain_length}{MCMC length per chain.}

\item{burn_in}{Burn-in per chain.}

\item{keep_burnin}{If \code{TRUE}, retain burn-in draws for diagnostic
plots over the full chain. Point-estimation functions still discard
\code{burn_in} draws by default.}

\item{chain_num}{Number of independent chains.}

\item{a_s0, b_s0, a_g0, b_g0}{Beta prior hyperparameters for slip and guess.}

\item{p_add}{Proposal probability for adding an edge in the hierarchy
\code{G} update; removing an edge is proposed with probability
\code{1 - p_add} (default 0.5).}

\item{parallel}{If \code{TRUE}, run chains in parallel.}

\item{n_cores}{Number of worker processes used when \code{parallel = TRUE}.}

\item{progress}{Whether to print per-chain iteration progress messages.}

\item{print_every}{Print progress every this many MCMC iterations.}

\item{progress_callback}{Optional function used by interactive front ends.
When supplied, it is called as \code{progress_callback(current, total,
model, final)} whenever the main process refreshes per-chain progress.}
}
\value{
A list of chain outputs with model settings and posterior draws.
}
\description{
\code{AHM} is the fixed-Q counterpart of \code{\link{AHMQ}}. It estimates
item parameters, individual attributes, class probabilities, and the
attribute hierarchy while treating the supplied Q-matrix as known. Because Q
anchors the attribute labels, the default post-processing for \code{AHM}
output does not perform Q-based label-switch correction and does not report
Q recovery metrics.
}
