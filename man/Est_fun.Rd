% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Est_fun.R
\name{Est_fun}
\alias{Est_fun}
\title{Point estimates from multi-chain \code{AHMQ} output}
\usage{
Est_fun(
  result,
  burn_in = NULL,
  chain_length = NULL,
  cut_value = 0.2,
  compute_rhat = TRUE,
  rhat_parameters = c("s", "g"),
  return_samples = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{result}{Output of \code{AHMQ} (list of chains, each with \code{data}
and \code{chain_sample}).}

\item{burn_in}{Burn-in columns to drop; \code{NULL} uses all stored draws.
If the \code{AHMQ} result was created with \code{keep_burnin = TRUE},
\code{NULL} automatically uses the burn-in value recorded in the result.}

\item{chain_length}{Total stored iterations; \code{NULL} inferred from data.}

\item{cut_value}{Threshold on posterior mean of \code{G}; \code{NULL} uses
the package default \code{0.2}.}

\item{compute_rhat}{If \code{TRUE}, compute Rhat diagnostics across all
chains before DIC chain selection.}

\item{rhat_parameters}{Chain sample components for \code{\link{compute_Rhat}}.
Only \code{"s"} and \code{"g"} are used.}

\item{return_samples}{If \code{TRUE}, include the DIC-selected, relabeled
posterior samples in the returned object.}

\item{verbose}{If \code{TRUE}, print label-switch progress.}
}
\value{
A list with \code{Est_s}, \code{Est_g}, \code{Est_pi}, \code{Est_G},
  \code{Est_Q}, \code{Est_alpha}, \code{Est_GG}, \code{DIC},
  \code{DIC_all}, \code{best_chain}, and optionally \code{Rhat} and
  \code{posterior_samples}.
}
\description{
Post-processes the list returned by \code{AHMQ}: selects the chain with
minimum DIC, resolves attribute label switching, binarizes parameters, and
returns hierarchy-consistent Q-matrix estimates.
}
\seealso{
\code{\link{select_chain_by_DIC}}, \code{\link{resolve_label_switch}}
}
