% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AHMQ_main_fun.R
\name{AHMQ}
\alias{AHMQ}
\title{Multi-chain DINA estimation with AHM Q-matrix prior}
\usage{
AHMQ(
  Y,
  K,
  N1 = NULL,
  chain_length = 20000L,
  burn_in = 10000L,
  keep_burnin = FALSE,
  chain_num = 4L,
  a_s0 = 1,
  a_g0 = 1,
  b_s0 = 1,
  b_g0 = 1,
  p_add = 0.5,
  parallel = TRUE,
  n_cores = min(chain_num, parallel::detectCores(logical = FALSE)),
  progress = TRUE,
  print_every = 1000L
)
}
\arguments{
\item{Y}{An \eqn{N \times J} binary response matrix.}

\item{K}{Number of attributes.}

\item{N1}{Mini-batch sample size for updating item parameters, Q, and G.
If \code{NULL}, the default is \code{floor(N / 2)}, where \code{N} is the
number of examinees.}

\item{chain_length}{MCMC length per chain (default 20000).}

\item{burn_in}{Burn-in per chain (default 10000).}

\item{keep_burnin}{If \code{TRUE}, retain burn-in draws in
\code{chain_sample}. This is useful for diagnostic plots over the full
chain. Point-estimation functions still discard \code{burn_in} draws by
default when this flag is present.}

\item{chain_num}{Number of independent chains (default 4).}

\item{a_s0, b_s0, a_g0, b_g0}{Beta prior hyperparameters for slip and guess.}

\item{p_add}{Proposal probability for adding an edge in the hierarchy
\code{G} update; removing an edge is proposed with probability
\code{1 - p_add} (default 0.5).}

\item{parallel}{If \code{TRUE}, run independent chains in parallel with
\code{parallel::mclapply} on Unix-like systems or a PSOCK cluster on
Windows.}

\item{n_cores}{Number of worker processes used when \code{parallel = TRUE}.}

\item{progress}{Whether to print per-chain iteration progress messages.}

\item{print_every}{Print progress every this many MCMC iterations.}
}
\value{
A list of length \code{chain_num}. Each element is a list with
  \code{data} (settings) and \code{chain_sample} (posterior draws:
  \code{G}, \code{s}, \code{g}, \code{Q}, \code{pi}, \code{alpha}).
}
\description{
Runs \code{chain_num} independent MCMC chains. Each chain draws its initial
values inside the C++ sampler and returns posterior draws together with the
analysis settings used.
}
